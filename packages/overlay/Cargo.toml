[package]
name = "lanplay-overlay"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Session metrics model for the in-stream overlay"

[dependencies]
lanplay-shared = { path = "../shared" }
serde = { workspace = true }
