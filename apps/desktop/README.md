# LANPlay desktop

Tauri 2 + React + TypeScript shell.

```powershell
npm install
npm run tauri dev    # development
npm run tauri build  # .exe + NSIS installer
```

See root [README](../../README.md) for prerequisites.
